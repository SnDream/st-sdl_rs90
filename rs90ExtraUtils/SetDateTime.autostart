#!/bin/sh

source ~/.profile 2>/dev/null

[ -e /var/tmp/.setdatetime_ok ] || {
	touch /var/tmp/.setdatetime_ok
	if [ -e "${OPK_FILE}" ]
	then opkrun -m settimedate.hide.desktop "${OPK_FILE}"
	else rm -f $0 ; exit 0
	fi
}

exec /usr/bin/gmenu2x
